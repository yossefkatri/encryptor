package multiThreading;

import keys.IKey;

import java.nio.file.Path;

public class AsyncDirectoryProcessor implements IDirectoryProcessor{
    @Override
    public void encryptDirectory(Path originalDirPath, Path outputDirPath, IKey key) throws Exception {

    }

    @Override
    public void decryptedDirectory(Path originalDirPath, Path outputDirPath, IKey key) throws Exception {

    }
}
